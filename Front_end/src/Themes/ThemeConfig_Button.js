import {createMuiTheme, ThemeProvider} from '@material-ui/core/styles'

const ThemeConfig_Button = createMuiTheme ({
    
    palette: {
        primary: {
            main : '#701919'
        }
    }
})

export default ThemeConfig_Button;